{
  "name": "bienestar-digital-workspace",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "unrs-resolver"
    ],
    "supportedArchitectures": {
      "os": ["current", "linux"],
      "cpu": ["current", "x64", "arm64"],
      "libc": ["current", "musl"]
    }
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": [
      "pnpm --filter chatbot exec eslint --fix"
    ],
    "backend/**/*.{js,ts}": []
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5"
  },
  "scripts": {
    "prepare": "husky"
  }
}
